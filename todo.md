# The never ending todo

- [ ] Watch all css files
- [ ] Start watching html files
- [ ] Yeet, just watch all files other than .py and .txt .json 
- [ ] By default add the js file to all html files when sreved using render_template()
- [ ] Cut off and restart after n number of detected changes (Maybe 1000?)
- [ ] Make this a wrapper over app like
    ```py
    import watcher
    watcher(app)
    ```
- [ ] If everything works, publish in pip (Daring today, aren't we?)